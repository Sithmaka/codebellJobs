rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: For demo only. Lock these down in production.
    match /{document=**} { allow read, write: if true; }
  }
}
